#include<stdio.h>

int main()

{

    int t;
    scanf("%d", &t);
    while(t--)
    {
        int n;
        long long s;
        long long sum = 0;
        scanf("%d %lld", n, s);
        for(int i = 1; i <= n; i++)
        {
            
            sum = sum + i;
            printf("%d ", )
            
        }

    }

    return 0;
}